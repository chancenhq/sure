<%= render DS::Button.new(
      text: current_partner&.translation(:onboarding, :header, :sign_out, default: t("partner_onboardings.header.sign_out")),
      icon: "log-out",
      icon_position: :right,
      variant: "ghost",
      href: session_path(Current.session),
      method: :delete
    ) %>
