<% 
  header_title t(".heading")
%>
<div class="mt-6 sm:mt-10">
  <div class="max-w-xl mx-auto space-y-8 bg-surface-inset border border-secondary rounded-2xl p-6 text-primary">
    <div class="space-y-2 text-center">
      <p class="text-secondary text-base leading-relaxed"><%= t(".subheading") %></p>
      <p class="text-secondary leading-relaxed"><%= t(".body") %></p>
    </div>

    <div class="space-y-3 text-left">
      <h3 class="text-lg font-medium text-primary"><%= t(".key_points_heading") %></h3>
      <ul class="list-disc space-y-2 text-secondary pl-6">
        <li><%= t(".points.isa") %></li>
        <li><%= t(".points.budgeting") %></li>
        <li><%= t(".points.skills") %></li>
      </ul>
    </div>

    <div class="pt-4">
      <%= render DS::Link.new(text: t(".cta"), href: privacy_reg_chancen_path, variant: :primary, full_width: true) %>
    </div>
  </div>
</div>
